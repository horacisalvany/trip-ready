BUG:
Sections aren't deletable

Description:
The application doesn't allow to delete a section of a list. either it happens in mobile and desktop.

Actual result:
When I drag a section from a list and drop it on the trash, the section keeps floating in the screen.

Expected result:
When I drag a section from a list and drop it on the trash, the section is removed from the list.
